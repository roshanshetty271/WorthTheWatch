# Worth the Watch? — Backend Dependencies
# Python 3.11+

# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Validation & Settings
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP client (async)
httpx>=0.27.0

# LLM (OpenAI-compatible SDK — works with DeepSeek too)
openai>=1.50.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Web scraping (article reader)
beautifulsoup4>=4.12.0

# Robustness
fake-useragent>=1.5.1

# XML/HTML Parsing (Speed)
lxml>=5.0.0

# Fuzzy Search (Typo Handling)
rapidfuzz>=3.11.0
selectolax>=0.3.21
