# Worth the Watch? — Backend Dependencies
# Python 3.11+

# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Validation & Settings
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP client (async)
httpx>=0.27.0

# LLM (OpenAI-compatible SDK — works with DeepSeek too)
openai>=1.50.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
